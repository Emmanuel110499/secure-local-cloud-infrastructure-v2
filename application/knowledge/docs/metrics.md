# Les métriques

Une métrique est une mesure chiffrée permettant de suivre l’état ou les performances d’un système.

Dans cette plateforme, les métriques permettent de surveiller :

- l’utilisation du CPU ;
- la mémoire RAM utilisée et disponible ;
- l’espace disque ;
- le trafic réseau ;
- la consommation des conteneurs Docker ;
- la disponibilité des services.

# CPU et RAM

La métrique CPU indique la charge du processeur. Elle permet de détecter une surcharge, un processus trop gourmand ou un serveur manquant de puissance.

La métrique RAM indique la quantité de mémoire utilisée et disponible. Elle permet de détecter un manque de mémoire ou un service consommant trop de ressources.

Node Exporter expose les métriques du serveur. cAdvisor expose celles des conteneurs. Prometheus les collecte et Grafana les transforme en graphiques.

# Target Prometheus DOWN

Une cible Prometheus DOWN signifie que Prometheus ne parvient plus à récupérer les métriques du service.

Les causes possibles sont :

- un service arrêté ;
- une mauvaise adresse IP ;
- un port fermé ;
- un problème réseau ;
- un pare-feu ;
- une configuration Prometheus incorrecte.1~# Les métriques

Une métrique est une mesure chiffrée permettant de suivre l’état ou les performances d’un système.

Dans cette plateforme, les métriques permettent de surveiller :

- l’utilisation du CPU ;
- la mémoire RAM utilisée et disponible ;
- l’espace disque ;
- le trafic réseau ;
- la consommation des conteneurs Docker ;
- la disponibilité des services.

# CPU et RAM

La métrique CPU indique la charge du processeur. Elle permet de détecter une surcharge, un processus trop gourmand ou un serveur manquant de puissance.

La métrique RAM indique la quantité de mémoire utilisée et disponible. Elle permet de détecter un manque de mémoire ou un service consommant trop de ressources.

Node Exporter expose les métriques du serveur. cAdvisor expose celles des conteneurs. Prometheus les collecte et Grafana les transforme en graphiques.

# Target Prometheus DOWN

Une cible Prometheus DOWN signifie que Prometheus ne parvient plus à récupérer les métriques du service.

Les causes possibles sont :

- un service arrêté ;
- une mauvaise adresse IP ;
- un port fermé ;
- un problème réseau ;
- un pare-feu ;
- une configuration Prometheus incorrecte.1~# Les métriques

Une métrique est une mesure chiffrée permettant de suivre l’état ou les performances d’un système.

Dans cette plateforme, les métriques permettent de surveiller :

- l’utilisation du CPU ;
- la mémoire RAM utilisée et disponible ;
- l’espace disque ;
- le trafic réseau ;
- la consommation des conteneurs Docker ;
- la disponibilité des services.

# CPU et RAM

La métrique CPU indique la charge du processeur. Elle permet de détecter une surcharge, un processus trop gourmand ou un serveur manquant de puissance.

La métrique RAM indique la quantité de mémoire utilisée et disponible. Elle permet de détecter un manque de mémoire ou un service consommant trop de ressources.

Node Exporter expose les métriques du serveur. cAdvisor expose celles des conteneurs. Prometheus les collecte et Grafana les transforme en graphiques.

# Target Prometheus DOWN

Une cible Prometheus DOWN signifie que Prometheus ne parvient plus à récupérer les métriques du service.

Les causes possibles sont :

- un service arrêté ;
- une mauvaise adresse IP ;
- un port fermé ;
- un problème réseau ;
- un pare-feu ;
- une configuration Prometheus incorrecte.1~# Les métriques

Une métrique est une mesure chiffrée permettant de suivre l’état ou les performances d’un système.

Dans cette plateforme, les métriques permettent de surveiller :

- l’utilisation du CPU ;
- la mémoire RAM utilisée et disponible ;
- l’espace disque ;
- le trafic réseau ;
- la consommation des conteneurs Docker ;
- la disponibilité des services.

# CPU et RAM

La métrique CPU indique la charge du processeur. Elle permet de détecter une surcharge, un processus trop gourmand ou un serveur manquant de puissance.

La métrique RAM indique la quantité de mémoire utilisée et disponible. Elle permet de détecter un manque de mémoire ou un service consommant trop de ressources.

Node Exporter expose les métriques du serveur. cAdvisor expose celles des conteneurs. Prometheus les collecte et Grafana les transforme en graphiques.

# Target Prometheus DOWN

Une cible Prometheus DOWN signifie que Prometheus ne parvient plus à récupérer les métriques du service.

Les causes possibles sont :

- un service arrêté ;
- une mauvaise adresse IP ;
- un port fermé ;
- un problème réseau ;
- un pare-feu ;
- une configuration Prometheus incorrecte.1~# Les métriques

Une métrique est une mesure chiffrée permettant de suivre l’état ou les performances d’un système.

Dans cette plateforme, les métriques permettent de surveiller :

- l’utilisation du CPU ;
- la mémoire RAM utilisée et disponible ;
- l’espace disque ;
- le trafic réseau ;
- la consommation des conteneurs Docker ;
- la disponibilité des services.

# CPU et RAM

La métrique CPU indique la charge du processeur. Elle permet de détecter une surcharge, un processus trop gourmand ou un serveur manquant de puissance.

La métrique RAM indique la quantité de mémoire utilisée et disponible. Elle permet de détecter un manque de mémoire ou un service consommant trop de ressources.

Node Exporter expose les métriques du serveur. cAdvisor expose celles des conteneurs. Prometheus les collecte et Grafana les transforme en graphiques.

# Target Prometheus DOWN

Une cible Prometheus DOWN signifie que Prometheus ne parvient plus à récupérer les métriques du service.

Les causes possibles sont :

- un service arrêté ;
- une mauvaise adresse IP ;
- un port fermé ;
- un problème réseau ;
- un pare-feu ;
- une configuration Prometheus incorrecte.1~# Les métriques

Une métrique est une mesure chiffrée permettant de suivre l’état ou les performances d’un système.

Dans cette plateforme, les métriques permettent de surveiller :

- l’utilisation du CPU ;
- la mémoire RAM utilisée et disponible ;
- l’espace disque ;
- le trafic réseau ;
- la consommation des conteneurs Docker ;
- la disponibilité des services.

# CPU et RAM

La métrique CPU indique la charge du processeur. Elle permet de détecter une surcharge, un processus trop gourmand ou un serveur manquant de puissance.

La métrique RAM indique la quantité de mémoire utilisée et disponible. Elle permet de détecter un manque de mémoire ou un service consommant trop de ressources.

Node Exporter expose les métriques du serveur. cAdvisor expose celles des conteneurs. Prometheus les collecte et Grafana les transforme en graphiques.

# Target Prometheus DOWN

Une cible Prometheus DOWN signifie que Prometheus ne parvient plus à récupérer les métriques du service.

Les causes possibles sont :

- un service arrêté ;
- une mauvaise adresse IP ;
- un port fermé ;
- un problème réseau ;
- un pare-feu ;
- une configuration Prometheus incorrecte.1~nano knowledge/docs/metrics.md
